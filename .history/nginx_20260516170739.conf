server {
    listen 80;
    location /uploads/ {
        alias /app/uploads/;
        expires 30d;
        add_header Cache-Control "public";
    }
}docker cp project_skywind-product-service-1:/app/uploads ./temp_uploads