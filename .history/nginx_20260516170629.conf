server {
    listen 80;
    location /uploads/ {
        alias /app/uploads/;
        expires 30d;docker inspect project_skywind-product-service-1 --format "{{json .Mounts}}"
        add_header Cache-Control "public";
    }
}